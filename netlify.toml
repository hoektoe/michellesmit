[build]
  command = "npm run build"
  publish = "out"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[context.production]
  environment = { NODE_VERSION = "15.3.0", NETLIFY_USE_YARN = "true", NODE_ENV = "production" }
